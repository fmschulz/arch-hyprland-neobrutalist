# Wallpapers

This repo ships a single wallpaper:

- `wallpaper.png` - the currently active wallpaper from the source setup

`wallpaper-cycle.sh` still works, but with one shipped image it simply reapplies the same background.
